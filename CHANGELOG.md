# 更新记录

## 2.0.14

**根治那一次可见的闪：让补注音和对方的重建发生在同一个任务里。**

### 轨迹量化了它

2.0.13 的会话（重启时间 17:41:50）里，250 行轨迹中 `ジオラマ` 被注音 **106 次**，
`changed=1 restored=1` 每秒约 **5 次、永不停止**：

```
17:39:58 changed=1 restored=1   17:39:59 changed=1 restored=1 (×4)
17:40:00 changed=1 restored=1 (×4)   17:40:01 …
```

也就是说：对方每 ~200ms 重建一次那一行（新 wrap），我们就补一次。
而我们是被 `MutationObserver` 叫醒的 —— **补的动作要等到下一帧才落地**，
中间那一帧画出来就是"没有注音"的样子。`age` 全都是 208~255ms（正常重绘量级），
所以 churn 闸门也不该拦它；问题纯粹在于**补得太晚**。

### 修法：补丁第五条，重建完直接叫我们

给 jp-furigana 的 `processLine` 末尾加一句：

```js
if (typeof window.__ktRepairLine === 'function') window.__ktRepairLine(line);
```

片假名终结者注册这个钩子，收到就**同步**把注音插回新 wrap。于是：

- 补注音和重建在**同一个任务**里，绘制时永远有注音 —— 那个空窗彻底消失；
- 对方的 observer 已经会忽略我们的变更（第三条补丁），插完不会反过来触发它重建，**不会自激**；
- 钩子里带重入保护，且我们会顺手清掉观测队列，不会多排一次 pass；
- 没打这条补丁 / 没装我们时，这一句就是空转，对别人没有任何影响。

`MutationObserver` 那条路（rAF 补）仍然保留，作为没打补丁时的退路。

### 验证

- `tools/patch-jp-furigana.js` 现在打 **5 处**，`tests/patch.test.js` 跟着固定 5 个锚点
- 新增集成测试「对方重建完一行直接叫我们时，注音要在同一次调用里补好（不能等下一帧）」
  —— 断言钩子**返回时**注音已经就位
- 已用 `--force` 从原始备份重打并核对包内五项齐全、`main.js` 语法正确
- 全部 **90 个用例通过**，`npm run check` 0 警告

## 2.0.13

**修「ジオラマ还是偶尔在闪」—— 两道闸叠在一起，变成"补两次就永久放弃"。**

### 先说一个容易看错的地方

上一版的轨迹我读错了会话：轨迹里的 `[boot]` 显示 **2.0.12 是 17:33:53 才加载的**，
而 15 条 churn 全发生在 17:32:17~17:33:39 —— **那是 2.0.11 的行为**
（`peer管=false` 的让位日志也印证了，那是 2.0.12 才改的）。

不过顺着那条轨迹查下去，发现 2.0.12 里确实还留着一个真问题。

### 问题：`reAnnotated` 和 churn 计数器两道闸

`decidedByHost` 里的 `reAnnotated` 是当年为了防抽搐加的："文字没变的话补一次，
再被丢就不补了"。2.0.12 又加了 churn 计数器（按 `age` 判断是不是真死循环）。
两道闸叠起来的结果，用调试脚本一跑就看清了：

```
第 1 轮: changed=1  → 注音在
第 2 轮: changed=1  → 又补上
第 3 轮: skipped=1 changed=0   ← reAnnotated 硬挡，永久放弃
```

而此时 `age≈220ms` 又被 churn 闸门判为"正常重绘、不计数"，于是**进不了认输期，
也不会再补**。要等到换行或设置变动（`restoreAll`）才回来一次 ——
**这就是"偶尔闪一下"**。

**改法**：只保留一条硬判断 —— **`hostHasOurRuby(host)`，注音还在就算"不用管"**。
注音没了就往下重新注音；要不要继续跟下去**统一交给 churn 计数器**
（它按 `age` 区分真死循环与正常重绘）。`reAnnotated` 那道重复的闸去掉。

于是行为变成：

| 情况 | age | 行为 |
| --- | --- | --- |
| 对方重写元素、抹掉注音（正常重绘） | ~200ms+ | 每次都补，**在下一帧内完成，看不见** |
| 对方注什么秒毁什么（真死循环） | ~0ms | 记满 3 次进认输期，安静下来 |

旧的那条契约测试「React 每轮都用同样的文字重建整行时，必须很快停手」仍然通过
（那种情况 age≈0，会被计数并停手）。

### 验证

- 新增「元素没换、只是注音被抹掉时，age 也要算得出来（否则闸门失效）」
  —— 在旧代码上确实失败
- 全部 **89 个用例通过**，`npm run check` 0 警告

## 2.0.12

**修「关了 jp-furigana 就几乎不注音」—— 让位条件写错了，整个方向反了。**

### 你的观察直接指向了它

> 关了 furigana 后 RNP 歌词页只有クローバー有注音，而网易云默认播放页是全都没注音

- クローバー 那行是 `いざなったクローバー` —— **一个汉字都没有**
- 其它行全都有汉字（`馴染`/`輝`/`煌`/`待`/`開`）

轨迹里 `未注音 汉字让位 peer管=false` 出现了 **400+ 次**，样例就是
`"作词: いよわ" / "作曲: いよわ" / "编曲: いよわ"`。原因在这一句：

```js
if (lineHasKanji(lineEl) && !(coexistWithFurigana() && isFuriganaManaged(lineEl)))  跳过
```

按德摩根展开就是 `hasKanji && (!coexist || !managed)` —— **共存开关默认是关的**，
于是"含汉字"就等于"整行让开"，**哪怕 jp-furigana 根本没管这一行**
（被关掉、没装、或者还没来得及处理）。关掉 jp-furigana 之后整页没注音就是这么来的。

**改法**：让位要三条同时成立 —— 含汉字、**对方正在管这一行**、共存开关没开：

```js
if (lineHasKanji(lineEl) && isFuriganaManaged(lineEl) && !coexistWithFurigana())
```

对方没在管的行跟我们没有冲突，没有理由让。

### 顺便：认输机制不该为"正常重绘"触发

轨迹里的 `age` 把两类现象分得很清楚：

| age | 含义 | 该怎么办 |
| --- | --- | --- |
| 223ms / 435ms / 543ms | RNP 分几次补齐歌词行，**正常重绘** | 补回来就完了（一帧的事，看不见） |
| ≈0~几十 ms | 对方无条件重建，注什么秒毁什么 | 这才是真死循环，值得退避 |

所以现在**只有"刚插上就被毁"（age < 150ms）才计入打架**。这正是
「RNP 页的ジオラマ一直没注音」的原因：它其实每次都被补回来了，
只是我的认输机制把它按下去十几秒。

### 契约调整（两条老测试跟着改）

- 「含汉字的歌词行整个让给振假名插件」→ 改成
  「**对方正在管的**含汉字歌词行整个让给它」+ 新增
  「对方没管的含汉字行要照标」
- 集成测试同样拆成两条：没有振假名插件时含汉字行要标 / 对方接管了才让开

### 验证

- 新增「注音活了半秒才被重建 = 正常重绘，不该进入认输期」—— 在旧代码上失败
- 全部 **88 个用例通过**，`npm run check` 0 警告

## 2.0.11

**「只有某一个词没注音」查清了，是两个真 bug —— 都在我们这边。**

### 轨迹给的现场（2.0.10）

```
17:14:27 已注音 region=DIV.rnp-lyrics-line.passed host=DIV.rnp-lyrics-line-original 文本="いざなったクローバー"
17:14:27 标记区域 ... 文本="いざなったクローバーcloveri za na "     ← 注进去了
17:14:28 已注音 ... 文本="ジオラマに" / [pass] changed=1 restored=1
17:14:29 churn 放弃 1000ms 文本="ジオラマに" age=435ms peer{dirty=false 无wrap=0 ktOwn=1 原文一致=true}
17:14:30 churn 放弃 2000ms 文本="ジオラマに" age=543ms peer{...}
17:14:33 churn 放弃 4000ms 文本="ジオラマに" age=521ms peer{...}
```

`未注音` 诊断**一条都没有** —— 说明那些词不是被跳过，是**注进去之后被抹掉**。
而 `age≈500ms` 告诉我们：注音活了半秒才没，不是死循环。
RNP 的歌词行出现后会分几次补齐（罗马音层陆续到达），每次重写都让
jp-furigana 重建该行、把我们的注音抹掉。

### Bug 1：重扫要等 250ms，那一闪就是这么被看见的

`schedule()` 默认防抖 250ms（≈15 帧）。被抹掉之后要过 15 帧才补回来，
肉眼就是一闪。改成：观测到 DOM 变更时用 `requestAnimationFrame`
**赶在下一帧绘制之前**补回去（并保留 40ms 最小间隔，避免自激成风暴）。
现在这一闪根本来不及显示。

### Bug 2：「底字没变」被当成了「注音还在」

```js
if (prior.annotated === visibleNow) { skipped++; continue; }
```

`visibleText()` **不包含注音**，所以注音被抹掉之后这个比较照样相等 ——
于是每一轮都跳过，**注音永远回不来**。默认播放页上「クローバー」的英文
一直不出现就是这个原因（RNP 在同一个元素里重写内容，元素没换、
底字一模一样，走的就是这条路）。

修法：这一比较额外要求 `hostHasOurRuby(host)` —— 真的还有 `<ruby class="kt-ruby">`
才算"不用管"。同时把这条重试路径也计入 churn，所以对方要是一直重写，
几轮之后仍会进入认输期停手，不会退化成"每轮都重注"的抽搐。

### 契约变更（有一条老测试因此改了）

「文字没变就**绝不**重注」这条契约是当年定的，那时重注要等 250ms、会明显闪。
现在补注音在下一帧内完成、且有 churn 认输兜底，所以契约改成
**有界重试**：允许补，但几轮之内必须停手。测试也跟着改成断言
"总重注次数 ≤ 4 且若干轮内收敛为 0 改动"，而不是"一次都不许"。

### 验证

- 新增集成测试「对方把注音抹掉后，必须在下一帧之前补回来（不能等 250ms 才补）」
- 两个修复**各自单独回退**都会让这条测试失败（分别验证过），说明两处都必要
- 全部 **85 个用例通过**，`npm run check` 0 警告

## 2.0.10

**给"某处没注上"补上跳过原因 —— 这一版只加诊断，不改行为。**

### 为什么现在只能加诊断

2.0.9 的真机轨迹里对上了用户的观察（RNP 歌词页只有ジオラマ没注音）：

```
17:10:55 region=LI.line                已注音 " ジオラマに" ✓ "くステージに" ✓ "めくライトも　" ✓ "たなステージを" ✓ "　ドアを" ✓
17:12:55 region=DIV.rnp-lyrics-line    已注音               "くステージに" ✓ "めくライトも " ✓ "たなステージを" ✓ " ドアを" ✓
```

**RNP 页上唯独没有 ` ジオラマに` 这一条**，同一段文字在默认页上却注上了
（` ジオラマdioramaに馴染なじんだ思おもい出`）。所以不是"没译文"、也不是缓存问题。

但**我看不到它被跳过的原因**：日志只记录"成功注音"。能导致整段不注音的路有
好几条（没译文 / 按行让位 / 在认输期 / 文本在动 / 太短 / 制作信息行），
以前一条痕迹都不留 —— 这几轮我反复猜错，就是这个原因。

所以这一版把每条路都写进轨迹（每轮最多 6 条）：

```
未注音 无译文 @DIV.rnp-lyrics-line " ジオラマに" 词=ジオラマ | 汉字让位 peer管=false @LI.line "..." 宿主=SPAN.fg-line
```

其中 **`汉字让位 peer管=false`** 是最值得看的一条：它说明"这一行含汉字、
jp-furigana 又没管它" —— 按现在的规则我们会整行让开，于是整行都不注音，
这正是"只有某一个词没有"的典型成因。

两条回归测试固定住这个诊断：
- 「没有译文的片段必须留下「跳过原因」，不能无声无息」
- 「按行让位时也要写明「对方到底管没管这一行」」

### 顺便修好了轨迹读取器

`tools/read-trace.js` 之前会挑到**最旧**的那份轨迹（按"最长"取值，而 leveldb
会把旧值留在别的 `.ldb` 里）。改成按**最后一行的时间戳**取最新，并逐文件打印：

```
--- 000588.ldb (5709B) 条目 4，轨迹 16085 字符，最后一行 17:13:01
--- 000586.ldb (6002B) 条目 4，轨迹 16729 字符，最后一行 17:10:01
```

## 2.0.9

**「ライト的英文一直没有闪过，现在ジオラマ的英文消失了」—— 是我的退避策略误伤。**

### 证据

先把轨迹读出来（这一步以前一直读不到，见下面的工具修复），2.0.8 的轨迹：

```
17:04:09 churn 放弃这一行 2s   ... 文本="ジオラマに" peer{dirty=false hosts=1 无wrap=0 mirrors=0 ktOwn=1 原文一致=true}
17:04:12 churn 放弃这一行 600s ... 文本="ジオラマに" peer{dirty=false hosts=1 无wrap=0 mirrors=0 ktOwn=1 原文一致=true}
```

两个结论：

1. **`原文一致=true`** —— 2.0.8 的第四条补丁已经把 `hostsText()` 那个问题修掉了，
   我原来怀疑的"注音污染原文比对"不再是原因；对端状态完全健康
   （`isClean()` 必然为真），**重建这一行的不是 jp-furigana**；
2. **两次 churn 隔了 3 秒** —— 那是正常重绘（行切换 / 逐字动画）的量级，
   不是死循环（1.2.3 之前的实测是每秒 4 轮）。可 2.0.6 的策略是
   "第二次起直接退到 10 分钟"，于是就出现了「英文消失十分钟」。

### 改法

| 参数 | 2.0.8 | 2.0.9 | 理由 |
| --- | --- | --- | --- |
| 首次退避 | 2s | **1s** | 正常重绘要尽快重试，试一次就稳 |
| 递增 | 第 2 次直接跳上限 | **每次翻倍**（1s→2s→…→10min） | 真死循环仍会在一分钟内退到上限，正常重绘不会 |
| 原谅期 | 无 | **10s 不打架就清零 strikes** | 否则一段文字被打过一次之后，之后每次重试都只给 1 轮机会、起点更高 |

**诊断增强**：churn 日志新增 `age=`（我们的注音活了多久）。以后一眼就能分辨
"死循环"（几十毫秒就没）和"正常重绘"（活了一两秒）。

### 工具修复：轨迹终于能读了

之前卡在三个坑上，这次都解决了（`tools/read-trace.js` 重写）：

1. 直接读 `.log`：客户端在写，复制时进程 `STATUS_HEAP_CORRUPTION` 崩掉；
2. 只读 `.ldb`：**块是 Snappy 压缩的**，只能搜到键、读出来是乱码
   （这就是"轨迹只有 1 行"的真相）；现在实现了 Snappy 解压 + leveldb SST 遍历
   （footer → index block → data block），读值按 Chrome 的编码标记解码（UTF-16LE）；
3. 4KB 窗口太小：值有 100KB+（250 行 × UTF-16），窗口根本不够。

现在 `node tools/read-trace.js` 能直接读出完整的 250 行轨迹。

### 回归测试

- 「短暂的几轮重绘之后要自己稳住 —— 不能一上来就退避十分钟」（这条正是本次事故）
- 「一直打个不停时，退避要按倍数退到上限」
- 两条都在 2.0.8 的策略下失败。

## 2.0.8

**根治了「ジオラマ一直闪」—— 补丁加了第四条，改的是 jp-furigana 的 `hostsText()`。**

### 根因

它判断"这一行还能用吗"靠这一条：

```js
return hostsText(line) === line.__fgText;
```

但两边的**算法不一致**：

| | 怎么算 | 算不算注音 |
| --- | --- | --- |
| `line.__fgText` | `plainText(line)` | 排除 `<rt>`/`<rp>`/`.fg-rt` |
| `hostsText(line)` | `Σ host.__fgOrig[].textContent` | **什么都算** |

只要我们的 `<ruby>` 在它 `applyWrap()` 之前就已经是宿主的子节点，它就会连我们的
ruby 一起存进 `__fgOrig`，于是 `hostsText` 里多出 rt 里的英文（`ステージstage`），
两边**永远**对不上 → `isClean()` 永远 false → 它每一轮 pass 都重建这一行 →
我们的注音每轮被抹掉 → 一直闪。

真机上什么时候会"我们比它先动手"？RNP 把当前唱到的那段换成纯文本、它的 wrap
那一瞬间不在了，我们正好补上注音。**而会闪的那几个词（ジオラマ、ライト）
恰好都不在离线词典里** —— 它们的注音是联网译文回来后补的，时机正好撞上。

### 复现与验证

`kt-work/tools/repro-orig-pollution.js` 用真实的 `annotate.js` + 照抄的
`applyWrap`/`restore`/`hostsText`/`plainText`/`isClean` 跑 6 轮：

| `hostsText` 口径 | 对端每轮重建次数 | 结果 |
| --- | --- | --- |
| `textContent`（现状） | `[1,1,1,1,1,1]` | `isClean` 永远 false，无限重建 |
| `plainText(n)`（我一开始想的天真修法） | `[1,1,1,...]` | **不行** —— TreeWalker 的 `nextNode()` 不访问根节点，传文本节点进去返回空串，会把正文丢掉 |
| 分三种情况（最终方案） | `[1,0,0,0,0,0]` | wrap 一次之后 `isClean` 恒为真，稳定 |

最终方案：文本节点取 `nodeValue`；外来注音（`kt-ruby`）只取它的底字、不要 rt；
其余元素走 `plainText`。

### 新增补丁工具的自检

`tests/patch.test.js`（5 个用例）用一段照抄真实源码的样本固定住**四处锚点**，
并断言：四处都能应用、打完补丁的代码语法正确、重复打是幂等的、
`--revert` 能逐字节还原、锚点缺失时中止且不改内容。
以后 jp-furigana 升级导致锚点失效，会在 CI 里直接红，而不是等用户发现闪。

> **需要重新打一次补丁**：`npm run patch:furigana -- --force`
> （`--force` 会以原始备份为基准重打，避免在旧补丁上叠加）。已替你执行完，
> 并确认包内四条补丁齐全、`main.js` 语法正确。

## 2.0.7

修上一版加的诊断（**只改诊断，不改行为**）。

2.0.6 的轨迹里 churn 日志输出的是 `peer=无标记` —— 读不到 jp-furigana 的状态。
原因：`describePeer()` 是顺着传进来的宿主往上找行元素，但调用它的位置是
`dropDetached()`，那时宿主**已经脱链**（parentElement 全是 null），自然找不到。

修法：注音时就把行元素存进记录（`rec.peerLine`，顺着祖先找带 `__fgText` 的那个），
出问题时直接用它。诊断字符串在合成环境里已经能正常输出：

```
churn 放弃这一行 60s（...）文本="コーヒーを"
  peer{dirty=false hosts=0 无wrap=0 mirrors=0 ktOwn=- 原文一致=false orig="" fgText="コーヒーを飲みながら"}
```

> 上面的 `hosts=0` 是合成环境没造 `__fgHosts` 导致的，真机上会是真实数值。
> 关键看 `原文一致`：这条判据用的是 `host.__fgOrig` 的 textContent 求和，
> 我们的 `<rt>` 文字会被算进去 —— 如果真机上它是 `false`，就说明我们的注音
> 污染了 jp-furigana 的"原文"比对，那一行会永远判定为脏、永远重建，
> 那才是"ジオラマ一直闪"的根因，需要再加一条补丁才能根治（而不是靠退避让开）。

顺带记录一下 2.0.6 的验证：真机轨迹里 2.0.6 生效前后有清晰的分界 ——

```
16:50:23 … 16:51:19  churn 放弃这一行 2s   × 14   ← 2.0.5（strikes 被删的旧行为）
──────────── 16:51:54 重启 ────────────
16:51:56  churn 放弃这一行 2s
16:51:58  churn 放弃这一行 600s                   ← 一次机会之后直接让开 10 分钟
```

用户确认："确实只闪一次"。

## 2.0.6

修 2.0.5 里的一个真 bug：**`strikes` 在认输时被删掉了，退避永远停在第 1 档。**

2.0.5 的打算是"第一次认输退 2s，之后按 4 倍递增"，但代码里认输那一步是
`churnByText.delete(text)` —— 连 `strikes` 一起丢了，于是每次重新计数都从 0 开始：

- 「已经判过它爱打架的片段，每次重试只试 1 轮」这段逻辑**从来没生效过**；
- 退避也永远停在第一档 2s。

结果就是用户看到的：**只有ジオラマ在闪** —— 每 2 秒重试一次，每次闪一下，
永远不升级、永远不停。其他片段都稳，所以只有它闪。

修法：认输时保留 `strikes`，只把窗口计数清零。

同时把策略收得更果断：**第一次认输退 2s（给对方"其实只是正在唱那一两秒"的机会，
唱完补上就稳了），第二次起直接顶到上限 10 分钟。**
不再按 4 倍递增（2s→8s→32s→2min→10min），因为每一档重试都会让那个片段再闪一下 ——
真能稳的 2s 后就稳了，稳不了的再试也没用。

**诊断增强**：churn 日志现在会**直接读出 jp-furigana 自己的状态**（只读它的
expando，不改任何东西）：

```
churn 放弃这一行 600s（...）文本="ジオラマに" peer{dirty=false hosts=3 无wrap=0
  mirrors=0 ktOwn=1 原文一致=true}
```

这几项正好对应它 `isClean()` 的判据。我怀疑 `原文一致` 这一条会被我们的 `<ruby>`
污染（它求和用的是 `host.__fgOrig` 的 `textContent`，我们的 rt 文字会被算进去，
于是那一行永远"不干净"、永远重建）—— 下次的轨迹会直接给出答案，
如果看到 `原文一致=false orig=...`，就是这个原因，那就得再加一条补丁。

**回归测试**新增「来回打第三次就长时间让开 —— 不能隔几秒闪一下」，
断言第二次认输直接退到 600s、且随后的重试确实不再发生。这条在 2.0.5 下失败。

## 2.0.5

继续修「行首没注上音」。2.0.4 把阈值调对之后，真机轨迹显示这个片段**确实在循环**
（不是误伤了）：

```
16:41:38 已注音 ... host=SPAN.fg-line 文本="ジオラマに"
16:41:39 已注音 ... host=SPAN.fg-line 文本="ジオラマに"
16:41:40 churn 放弃这一行 60s 文本="ジオラマに"
16:42:40 已注音 ... host=SPAN.fg-line 文本="ジオラマに"   ← 整整 60s 没有注音
16:42:41 churn 放弃这一行 60s 文本="ジオラマに"
```

把两份轨迹里所有 churn 的片段排在一起（`ジオラマに`、`めくライトも `、`くステージに`、
`たなステージを`）就能看出规律：**位置各不相同，但都是"当时正在唱的那个片段"。**
逐字动画会持续改写当前字所在的 DOM，我们注什么都会被抹掉 —— 但**只在那一两秒内**，
唱完就不动了。

所以 60s 的首次退避是错的：它整段错过了窗口，那一句直到滚走都没注音。改成：

| 参数 | 2.0.4 | 2.0.5 | 理由 |
| --- | --- | --- | --- |
| 首次退避 | 60s | **2s** | 唱完那一瞬间重试就能补上 |
| 递增 | ×4（60s→4min→10min） | ×4（2s→8s→32s→2min→10min） | 对方还在动的话迅速安静下来 |
| 已判过打架的片段每次重试试几轮 | 3 | **1** | 退避短 → 重试频繁，每多试一轮就多闪一次 |

效果：正在唱的那个词会晚 ~2 秒出现注音（唱完就补上，之后一直留着），
代价是那一瞬间可能闪一下；其余部分完全不受影响。

**回归测试**新增「认输只是暂时的：对方不再重建之后，注音要自己回来」——
先让它打架认输，等退避到期再扫一次，断言注音自己补回来、而且留在原位。
这条在 2.0.4（60s 退避）下确实失败。为了让测试不用真等 2 秒，
`churnBaseMs` 现在可以按选项覆盖。

## 2.0.4

修 2.0.3 引入的误伤：**歌词行刚变成当前行时被重绘两次，也会被当成"打架"而放弃 60s**
（用户反馈："有一处行首没注上音"）。

真机轨迹（2.0.3）：

```
16:36:09 已注音 region=...passed host=SPAN.fg-line 文本="ジオラマに"   ← 这行刚变成当前行
16:36:10 已注音 region=...passed host=SPAN.fg-line 文本="ジオラマに"   ← 被重绘掉，补一次
16:36:10 [pass] changed=1 restored=1
16:36:10 churn 放弃这一行 60s（注音反复被重建掉，宿主 SPAN.fg-line）文本="ジオラマに"
```

被放弃的 `文本="ジオラマに"` 正是那一行的**行首片段**（整行是
`ジオラマdioramaに馴染なじんだ思おもい出で`），所以那 60 秒行首没有注音。
但它只是"行切换时被重绘两次"，重绘完就稳定了 —— 不该放弃。

问题出在 2.0.2 定的阈值「4 秒内 2 次」太松（窗口太宽、次数太少）。改成拿真机数据对出来的两个数：

| 参数 | 2.0.3 | 2.0.4 | 依据 |
| --- | --- | --- | --- |
| `CHURN_WINDOW_MS` | 4000 | **1500** | 正常行切换就两次重绘，宽窗口会把它们累积起来 |
| `CHURN_LIMIT` | 2 | **3** | 真死循环在 1.5s 内稳定有 3 轮以上（1.2.3 之前的实测是 3 秒 5 轮） |

首次退避仍为 60s（真死循环认输后就该安静一阵），仍按 4 倍递增到 10min。

**回归测试**：新增「歌词行刚变成当前行、被重绘两次 —— 不能被当成打架而放弃」，
断言两次重绘后注音仍在原位、且不产生 churn 记录。这条在 2.0.3 的参数下确实失败。

## 2.0.3

**「有些闪，有些不闪」的成因查清了：闪的是正在逐字动画/滚动的那几行。**

2.0.2 的真机轨迹（这次带上了新诊断）关键几行：

```
16:29:03 [pass] regions=61 changed=0 restored=18          ← 18 个宿主整个消失，一个都没重注
16:29:14 [pass] regions=61 changed=10 restored=0 unstable=8
16:29:14 [pass] regions=61 changed=10 restored=10         ← 同一批行第二轮
16:29:15 [annotate] churn 放弃这一行 15s ... 文本="したストーリー"
```

- **`restored` 全部来自 `dropDetached()`，一条 `失效[...]` 都没有。** 也就是说
  闪烁不是判定逻辑出错，而是我们的注音**所在的元素被整个删掉重建**；
- 日志里的宿主既有 `host=DIV`（整行 `rnp-lyrics-line`），也有 `host=SPAN`
  （逐字碎片）—— 正在动画的那几行，DOM 每秒被重建好几轮，注音必然被抹掉；
  不在动画里的行没人动，就"不闪"。**这就是"有些闪，有些不闪"。**

这种行我们**追不上**（对方是主动重写 DOM，不是被我们触发的），唯一能做的就是更快松手。
所以这一版调了两个数：

| 参数 | 2.0.2 | 2.0.3 | 理由 |
| --- | --- | --- | --- |
| `CHURN_LIMIT`（认输前试几轮） | 4 | **2** | 每多试一轮就多闪一次 |
| `CHURN_BASE_MS`（首次退避） | 15s | **60s** | 15s 会让人一首歌里反复看到"闪一下、停、又闪一下" |

退避仍是按 4 倍递增（60s → 4min → 10min），对方不再重建后会自动恢复，不用重启插件。

**让"少标"变得可见**：开发模式的状态区会多一行
`已避让: N 行（对方反复重建，见轨迹里的 churn）` —— 这些行是插件**故意**不注音的，
不是漏了。

**诊断增强**：`已注音` 日志现在带上宿主的 tag+class（`host=DIV.rnp-lyrics-line` /
`host=SPAN.fg-line` …），`churn` 日志也会写明是哪个宿主被删掉 ——
下次再看轨迹就能直接区分"是我们的宿主被拆"还是"判定逻辑错了"。

## 2.0.2

**先说结论：这一版的闪烁不是本插件的 bug，是 jp-furigana 的共存补丁没了。**

真机轨迹（`tools/read-trace.js` 从网易云 Local Storage 里读出来的）第一行就是：

```
16:19:54 [pass] regions=61 changed=18 restored=18 skipped=3
```

这正是 1.2.3 修掉的那个签名。原因：jp-furigana 被换成了 **1.0.2**（`jp-furigana-1.0.2.plugin`，
15:57 写入），我打过补丁的 1.1.0 包已经不在，解包目录里的 `main.js` 一个补丁标记都没有。
**插件更新会覆盖补丁** —— 补丁是打在自己的 `.plugin` 包上的，包一换就没了。
重新执行 `npm run patch:furigana` 即可（1.0.2 的三处锚点和 1.1.0 完全一致，补丁照样能打）。

**本版新增：自动避让（认输机制）**，让这种失败模式不再表现为"一直抽搐"。

以前只要对方在无条件重建这一行，我们就会无限跟着重建。现在会数「同一段文字
被我们注了又被毁掉」的次数，短时间内超过 4 次就**主动认输**，停止处理这一行：

```
churn 放弃这一行 15s（文本没变却反复失效，八成是对方插件在无条件重建）文本="コーヒーを"
```

三个关键设计（都是踩出来的）：

- **判据按文字，不按元素。** 对方每次重建都新建一个 `<span>` 当宿主，我们的
  `rec.host` 就是它那个 wrap —— 按元素记永远归不了零。原来的
  `decidedByHost.reAnnotated` 防抖也是按元素的，所以对这个循环完全无效。
- **要连"宿主被整个摘掉"一起数。** 真正被触发的是 `dropDetached()`：
  对方把 wrap 删了，我们的记录成了孤儿，于是 `changed=1 restored=1` 每轮重复。
  只在"文本失效"那条分支上计数是数不到的（实际轨迹里连 `失效[...]` 日志都没有）。
- **正常换行不算。** 歌词真的换了一句时，`rec.plain` 每次都不同，计数各算一次，
  永远到不了阈值。退避时间按 4 倍递增（15s → 1min → 4min → 上限 10min），
  对方修好后会自动恢复，不需要重启插件。

**诊断改进**：`isStale()` 现在会写明是哪条分支判定失效
（`失效[宿主脱链]` / `失效[原文本节点被摘走]` / `失效[可见文本变了]`），
下次不用再靠猜。`tools/read-trace.js` 也修好了：先复制 leveldb 再解析
（直接读会 `STATUS_HEAP_CORRUPTION` 崩溃），并改成扫字符串字面量
（值会跨 log record 被截断，直接 `JSON.parse` 会失败）。

**回归测试**：`tests/coexist.test.js` 新增两条 —— 一条用"没打补丁的 jp-furigana"
模型复现 `changed+restored` 每轮重复，断言插件会认输停手（旧代码上确实失败）；
另一条断言正常换行不会被误判成打架。

## 2.0.1

修「以片假名开头的行，注音在行首和行尾来回横跳」。两个独立的坑，都在同一条路上。

**一、注音被写到宿主末尾（`annotate.js`）。**

文本以片假名词开头时，注入会先把原文本节点从宿主里摘掉，再插我们造的节点。
但插入位置是按 `node.nextSibling` 现算的 —— 节点摘掉之后这个值恒为 `null`，
`insertBefore(tail, null)` 等价于「挂到宿主末尾」：

```js
host.insertBefore(tail, leadIsRuby ? null : node.nextSibling);   // ← 摘完再算，恒 null
```

于是「コーヒーを飲みながら」这种行，コーヒー 的注音被排到 飲みながら 后面；
下一轮还原又把原节点按原下标放回行首，再注音又挪到行尾。位置在两头跳。

修法：**动 DOM 之前**先取好参照节点，两个分支都用它。顺带把记录里的
`index` 也改成动 DOM 之前算 —— 之前同样因为原节点已被摘掉而恒为 0，
还原时会把整段原文插到宿主行首。

**二、jp-furigana 暂存的旧注音被挂到错误位置（`annotate.js`）。**

补丁第二条让 jp-furigana 拆 wrap 前把我们的注音暂存到 `host.__ktForeign`，
我们取走后是这么挂回去的：

```js
host.insertBefore(fnode, node.nextSibling);   // node = 这一轮碰巧处理到的文本节点
```

`fnode` 属于一个刚被拆掉的 wrap，位置信息已经没了，挂到哪个 `node` 后面纯属碰运气 ——
行首的注音会被挂到行尾，而且 `__fgOrig` 一起还原回来的旧注音还在，于是**出现重复注音**。

修法：不再挂回去，只清空暂存。那一段的原文一定在 `__fgOrig` 里，`restore()` 已经把它
放回 DOM；紧接着的正常注音流程会按当前 DOM 重新标一遍，位置自然正确。

**回归测试**：`tests/coexist.test.js` 新增两条，忠实模拟 jp-furigana 的
`applyWrap` / `restore`（含补丁语义）跑多轮，断言注音位置和数量。
两条在旧代码上确实失败（8 pass / 2 fail）。

**jp-furigana 的补丁不用重打**，改的全是本插件侧。

## 2.0.0

**删掉浮层方案，只保留「按行分工 + 共存补丁」这一条路。**

1.2.x 里为了让本插件和 jp-furigana 互不干扰，同时存在两套渲染方式：
直接写进歌词行（`lyricRender: "inline"`）和把英文画在独立浮层上（`overlay`）。
共存补丁做完之后，浮层已经没有存在意义了 —— 它的全部优点（不碰歌词 DOM）
都被「按行分工」覆盖，代价（不参与排版、换行/缩放时位置偏）却是独有的。

- 删除 `src/core/overlay.js`（261 行）及其 9 个测试；
- 删除设置项 `lyricRender`（`configVersion` → 4，旧配置里的这个键会被忽略）；
- 含汉字的歌词行保持「让给 jp-furigana」的默认行为；
打开「与振假名插件共用同一行」（+ 打补丁）后同一行两种注音并存；
- 新增 `tests/coexist.test.js`（6 个用例），把共存行为固化成契约；
- 顺带修掉设置面板里「标注范围」标错默认值的问题（`all` 才是默认）；
- 顺带修掉 `coexistWithFurigana` **只在插件初始化时读一次**的 bug：
  这个开关以前勾了不生效（要重启网易云才行），删掉浮层后它是含汉字歌词行
  标上英文的唯一途径，所以一起修了。

不兼容变更：升级后含汉字的歌词行不会再出现英文注音，除非打开共存开关。
如果你之前一直在用浮层，这个版本会改变你看到的效果 —— 这是有意的。

## 1.2.3

继续修闪烁。这次找到了**真正的来源**：不是判定逻辑，而是**变更通知**。

jp-furigana 的 MutationObserver 回调里对任何 `childList`/`characterData` 变更
都会把最近的歌词行标脏：

```js
for (const r of records) {
  if (r.type !== 'characterData' && r.type !== 'childList') continue;
  for (let el = r.target; el; el = el.parentElement)
    if (el.__fgText != null) { el.__fgDirty = true; ... }   // ← 无条件标脏
```

我们插 `<ruby>` 恰好就是一次 `childList` 变更 → 该行被判脏 → `processLine` →
`restoreLine` 先把 wrap 摘掉（我们的注音随之消失）→ 重建 → 我们再插……

前两处补丁只影响「判定是否干净」，管不到「什么时候被标脏」，所以照样每轮重建。

补丁新增第三条（关键）：在 observer 回调最前面跳过我们引起的变更
（`__ktRecordIsOurs`）。配套地，插件会给**自己造出来的节点**打上 `__ktOwned`
标记（新建的文本分段 + 改写过的原文本节点），供它识别。

判断逻辑用 9 个用例验证过：

| 变更 | 视为我们的 |
| --- | --- |
| 插入/删除我们的 ruby | ✅ |
| 插入我们标记过的文本节点 | ✅ |
| 改写我们改过的原文本节点 | ✅ |
| 插入普通文本节点 | ❌ |
| 插入别人的 ruby | ❌ |
| 混合插入（我们的 + 普通的） | ❌ |

另外插件侧消费 `host.__ktForeign`：取走并**清空**它还原时摘下的注音节点，
立刻挂回原位（缩短可见空窗），同时避免节点越积越多。

## 1.2.2

修「歌词行消失」——**这是我 1.2.0 补丁里的 bug，我造成的。**

`restore()` 原本靠「host 为空」来决定是否放回原文字：

```js
if (!host.hasChildNodes() && host.__fgOrig && host.__fgOrig.length)
    host.append(...host.__fgOrig);
```

而我的补丁先把外来注音 `appendChild` 到 host 上，host 就有子节点了，
于是上面这个条件**永远不成立**，整行文字再也放不回来 —— 实测复现：

```
处理前:    取戻したい　ヒーローみたいに
restore 后: 只剩 [我们的 ruby]，其余文字全没了
原文本节点还在 p 里吗: false
```

**修法**：不再往 host 上挂节点，改成暂存到 host 的 expando（`host.__ktForeign`）：

```js
const __ktNodes = [...wrap.querySelectorAll('ruby.kt-ruby, .kt-ov-label')];
if (__ktNodes.length) host.__ktForeign = __ktNodes;
wrap.remove();        // 之后原有的 `if (!host.hasChildNodes() ...)` 依然成立
```

暂存后由片假名终结者自己接手，把节点挂回我们的原文本节点后面；
也不会在宿主上残留任何 expando。

修复前后对比（用真实函数实测）：

| | `restore()` 后可见文字 |
| --- | --- |
| 旧补丁 | `"ヒーロー"`（其余全丢） |
| 新补丁 | `"取戻したい　ヒーローみたいに"` ✅ |

顺带加固工具链：

- `revertPatch()` 改成按 `HELPER` 常量**精确移除**注入块（之前按行号找，留残渣），
  并支持传入原始文本做逐字节校验；
- `patch-jp-furigana-plugin.js` 新增 `--force`：以备份为基准重新打补丁，
  避免在旧补丁上叠加；
- `--revert` 优先用备份逐字节还原。

## 1.2.1

修「补丁打了但没用」。

原因很关键：**BetterNCM 每次启动都会把 `plugins/*.plugin` 重新解包到
`plugins_runtime/<slug>/`**，所以我上一版改解包目录里的 `main.js` 在下次启动时
被原封不动覆盖回去了（实测：改完是 53098 字节，重启后变回 63869 字节、
时间戳回到原日期，补丁消失）。补丁必须打在**包本身**上才能持久。

新增 `tools/patch-jp-furigana-plugin.js`：

- 直接修改 `plugins/jp-furigana-*.plugin`（读取 zip → 替换 `main.js` → 重新打包）；
- 支持 `--check` / `--revert` / `--file <路径>`；
- 打补丁前做语法自检，语法不过就中止且不改文件；
- 自动备份成 `<原名>.kt-bak`，只备份一次；
- 自带 zip 读写往返自检（20 个条目、内容逐字节一致），
  避免重新打包把词典文件弄坏。

`tools/patch-jp-furigana.js`（改解包目录的那个）保留，适合临时试验；
要持久请用新脚本。

**操作步骤**（改包之后必须让解包目录重新生成）：

```powershell
node tools/patch-jp-furigana-plugin.js
Remove-Item -Recurse -Force C:\betterncm\plugins_runtime\jp-furigana
# 然后重启网易云
```

## 1.2.0

**支持「同一行歌词里，汉字有振假名 + 片假名有英文」。** 需要给 jp-furigana 打补丁。

先把机制说清楚（用它的真实代码 + 真实 DOM 结构实测出来的）：

我原本以为冲突来自 `isClean()` 里的 `h.childNodes.length !== 1`。**实测发现不是**——
在"jp-furigana 已注音、我们插了 ruby"的场景下：

| | `isClean()` | `restore()` 后我们的注音 |
| --- | --- | --- |
| 未打补丁 | `true` | **0 个（被丢掉）** |
| 已打补丁 | `true` | **1 个（保住）** |

抹掉我们注音的真正原因是 `restore()`：

```js
const wrap = host.__fgWrap;
if (wrap && wrap.parentNode === host) wrap.remove();   // ← 我们的节点挂在 wrap 里，一起被扔了
```

它每次处理该行都会先 `restoreLine()`，所以我们的注音每轮都被丢掉一次。

另外实测到一件好事：我们改写的是它留着的原文本节点（只切短、不删除），
所以它算出来的「看得见的原文」`hostsText()` 一个字符都没变 ——
注解前后它读到的都是 `取戻したい　ヒーローみたいに`。

补丁做两件事（`tools/patch-jp-furigana.js`）：

1. `restore()`：拆 wrap 前，把我们挂在它 wrap 里的注音节点先搬到 host 上，
   免得跟着一起被丢掉（它随后重建 wrap 时会用 `include` 把 host 的全部子节点
   搬进新 wrap，我们的节点也跟着进新 wrap）；
2. `isClean()`：子节点计数忽略我们插的节点，作为纵深防御。

用法：

```bash
node tools/patch-jp-furigana.js           # 打补丁（自动备份 main.js.kt-bak）
node tools/patch-jp-furigana.js --check   # 看状态
node tools/patch-jp-furigana.js --revert  # 还原
```

补丁前会做语法自检，语法不过就中止且不改文件。锚点找不到会明确报出来，
不会打一半。

插件侧新增设置项 `coexistWithFurigana`（默认关）。打开它并且该行确认被
jp-furigana 接管时，含汉字的行也会注音；没打补丁时遇到它管的行仍然让开。

**注意**：jp-furigana 更新后补丁会丢失，重新跑一次脚本即可。

测试 71 个全过。

## 1.1.1

按「各管各的元素」重做与 jp-furigana 的共存（不再依赖浮层）。

关键发现：jp-furigana 的 `processLine()` 里有一条
`if (!FuriganaCore.hasKanji(text)) { line.__fgHosts = []; return; }`
—— **纯假名行它压根不管**。所以按「含不含汉字」分工就行：

- **含汉字的歌词行**：整个让给它，我们一个字节都不碰；
- **纯假名行**：归我们；
- **播放栏的歌曲名/歌手**：它从来不碰，我们照标（含汉字也标）。

于是 `lyricRender` 默认回到 `inline`（排版正确，不再是浮层），
靠这条分工规则避免冲突。浮层作为可选项保留（`lyricRender: "overlay"`），
适合"我全都要"的场景。

实现要点：

- `matcher.hasKanji()`：和 jp-furigana 同口径的汉字判断；
- `annotate.js` 新增 `skipKanjiLines` 选项（默认 `false`，不改变模块自身语义；
  由 `main.js` 在 inline 模式下传函数，每次扫描实时求值）；
- 新增 `isLyricRegion()`：只对**歌词行**做分工 —— 播放栏名称含汉字也要照标；
- 新增 `lineHasKanji()`：判断"看得见的原文"里有没有汉字。
  不能直接用 `textContent` —— 振假名插件插的 `<rt>` 文字也算 textContent，
  会把纯假名行误判成含汉字，导致该我们管的行反而被让出去。

修掉两个自己引入的 bug：`isLyricRegion` 最初把播放栏也判成歌词行（
`player-bar` 命中 `lyric`? 不，是判据过宽），以及循环里 `region` 变量
在使用之后才赋值。

测试 71 个全过。

## 1.1.0

**新增浮层渲染，可以和 jp-furigana（振假名插件）在同一行歌词上共存。**

先说清为什么必须换渲染方式。jp-furigana 的做法是：把整行内容换成自己的
`<span class="fg-line">`，然后靠 `h.childNodes.length !== 1` 判断「这行有没有被
外人动过」。我往它管的行里插 `<ruby>`，它就判定行脏 → 还原 → 重建整行 →
我的注音被抹掉 → 我再注……两边来回就是抽搐（轨迹实测 `changed=18 restored=18`
每秒四次）。**只要改歌词 DOM，就不可能共存。**

所以新增 `core/overlay.js`：把英文注音画在一个独立的浮层上。

- 完全不碰歌词 DOM（有测试断言歌词容器一字不变）；
- 用 `Range.getBoundingClientRect()` 量出每个片假名词的位置，绝对定位画上去；
- 浮层 `position:fixed` + `pointer-events:none`，不吃鼠标事件、不影响点击滚动；
- 滚动（捕获阶段，兼容内部滚动容器）、resize、DOM 变化、以及 250ms 兜底
  都会重新测量（逐字动画/transform 滚动不一定触发事件）；
- 只画视口附近的行，远处的不建标签。

设置里新增「歌词渲染方式」：

| 选项 | 说明 |
| --- | --- |
| `overlay`（默认） | 浮层，不碰 DOM，可与振假名插件共存 |
| `inline` | 直接写进歌词行，排版更准，但会和 jp-furigana 互相打架 |

默认范围回到 `all`（歌词用浮层 + 播放栏用 DOM 注音），配置迁移同步更新。

**如实说明代价**：浮层不参与排版，所以换行/缩放时位置可能略有偏差；
复制歌词不会带上英文注音；**定位效果我在开发环境里无法验证**
（jsdom 没有布局引擎），需要真机目视确认。

测试 61 → 71（新增 9 个浮层测试 + 1 个「歌词 DOM 逐字节不变」）。

## 1.0.11

针对「问题依旧」做了两件事：**默认不再碰歌词**，以及修掉一个导致开关失效的真 bug。

**1. 歌词标注改为默认关闭。**

真机上歌词行的 DOM 由网易云和 RefinedNowPlaying 高频重建，文字还带逐字动画，
注音进去会被反复丢掉。追着重注就是抽搐——这一点几轮都没能根治，
而它已经影响到正常使用，所以默认改成只标**播放栏的歌曲名/歌手**（DOM 稳定）。
需要歌词的人可以在设置里把「标注范围」改成"只标歌词"或"歌词 + 播放栏"。

**2. 修「关掉后仍在注音」的 bug（`core/annotate.js`）。**

```js
var list = regions && regions.length ? regions : findRegions("safe");
```

空数组是 truthy 但长度为 0，于是"没传区域"和"区域为空"被混为一谈：
传空数组反而会退回默认区域。结果就是把播放栏标注关掉后它还在注音。
改成 `regions ? regions : findRegions("safe")`。

**3. 新增「一直在变就不碰」的放弃机制。**

按 host 记录可见文本变化次数，连续变化超过 3 次就判定为"在动"并放弃，
不再追着重注（`unstable` 计数会出现在轨迹里）。稳定性优先于覆盖率。

**4. 没有区域时撤掉已有注音。**

关掉某个范围（区域列表变空）时，会把之前注的音还原，而不是留着不管。

验证（模拟真机：每轮扫描前把每行内部换新、文字不变）：

```
默认（只标播放栏）：pass1 changed=1 → pass2..5 changed=0        完全静止
打开歌词 + 每轮重建：pass1 changed=3 → pass2..6 changed=0       不再追击
```

测试 60 → 61。

**说明**：1.0.10 的改动我依赖的是构造的 DOM 模拟，而真机行为不同，
所以那次没能解决。这一版把默认行为改成不碰歌词，从源头上避免这个问题。

## 1.0.10

终于根治「歌词一直抽搐」。轨迹给出了确凿证据：

```
[pass] regions=121 changed=18 restored=18     ← 每 250ms，持续几分钟不停
```

18 行歌词在**无限地撤销 + 重注**。机制是：网易云/RefinedNowPlaying
会把歌词行的**内部子节点整批换新**，文字却一字不变。此时记录里的文本节点
已经作废，新文本节点没有记录，于是被当成"新的一行"重新注音 ——
React 一重建我们就注一次，来回触发，永不收敛。

改动（`core/annotate.js`）：

- 新增按 **host 元素**索引的记忆 `decidedByHost`：记住"这个 host 的这段
  可见内容已经处理过"，同时记下**两种可见形态**（注音在位时的底字、
  以及 React 丢掉我们节点后的底字）—— 这两种都说明内容已处理过。
- 判断顺序提到**还原之前**：否则即使最终决定不重注，中途也已经做了一次
  还原（一串 DOM 变更 = 可见的一闪）。
- React 已经把我们的节点丢掉时**不再"还原"**，直接丢记录重注 ——
  还原本身就会闪（`annotationsIntact()`）。
- 同一段内容最多补注一次（`reAnnotated`）：补了还被丢掉，说明补注音这个
  动作本身会触发对方重建，就不再补。宁可这一行暂时没有注音，也不动 DOM ——
  稳定性优先于覆盖率。
- `restoreAll()` 同时清掉这份记忆，否则用户手动禁用再启用（或改设置触发
  rescan）后会"怎么都不再注音"。

顺带修：轨迹里的 `[pass]` 日志改成只在真的做了事时才写。之前每 250ms
一条会把 250 行缓冲冲干净，导致我加的诊断日志（明明写了）在轨迹里根本
看不到 —— 白折腾一轮。

验证（模拟真机模式：每轮扫描前 React 把每行内部换新、文字不变）：

```
round1:    changed=2 restored=0
round2..8: changed=0 restored=0 skipped=2      ← 完全静止
```

改之前是 `changed=18 restored=18` 永不收敛。测试 58 → 60。

## 1.0.8

针对「还是抽搐」和「上一首的歌词出现在正在播放的歌词里」两个现象继续修。

后一个现象是最有价值的新线索：**页面上同时存在多个歌词容器** ——
正在播放的那个，加上上一首还没卸载的、以及 RefinedNowPlaying 的淡出副本。
之前的行为是「只要选择器命中就注音」，于是：

- 隐藏副本也被注音，换歌过程中两种歌词同时可见（就是"上一首的歌词串进来"）；
- 同一份歌词在多个容器里各注一遍，React 重建哪个我们就重注哪个，
  来回触发就是持续抽搐。

改动：

- `findRegions` / `collectBySelectors` 增加**可见性过滤**：明确隐藏
  （行内 `display:none` / `visibility:hidden`、`hidden` 属性，或
  `getComputedStyle` 能确认隐藏）的容器直接跳过。判定从严，拿不到样式时
  一律当可见，避免漏标正常内容。
- `pass()` 里原来有一个**全局的"失效记录还原"循环**：它每轮把**所有**
  历史记录无条件还原一遍（包括已经隐藏、根本不用管的行），然后才去处理区域。
  这才是抽搐一直没根除的原因。现在只在该记录对应的文本节点真的被处理到、
  且确认失效时才还原。
- 循环内节点所属区域也做同样的可见性判断。

顺带修测试里 `querySelector(...).length` 的写法（在本项目的 jsdom realm 下
不可靠），统一成 `querySelectorAll`。

新增测试：隐藏的歌词副本不标注、且多轮扫描稳定。

测试 57 → 58。

## 1.0.7

修「装了振假名插件（jp-furigana）时，同时有振假名和英文注音的歌词行仍会抽搐」。

根因是**跨插件的 class 冲突**：我用 `class="kt-region"` / `class="kt-fallback"`
标记已注音的区域，而这些区域元素（`ul.lyric > li`、RNP 的歌词行等）是和别的
歌词插件共用的。改它们的 `className` 会让对方的渲染检查判定"这行变了"并
重建整行，把我们的注音一起丢掉，下一轮我们再标一次 —— 两边互相触发，
表现就是只有"两种注音都有"的行才一直抽搐。

改法：标记一律改用 `data-kt-region` / `data-kt-fallback` 属性，
**不再写任何共用元素的 `className`**（只给自己创建的 `ruby` / `rt` / `span` 设 class）。
CSS 选择器同步改成 `[data-kt-region]` / `[data-kt-fallback]`。

新增回归测试：扫描前后，所有既有元素的 class 必须一字不变。

测试 56 → 57。

## 1.0.6

修「含片假名的歌词一直抽搐」。轨迹里是同一批注音在反复重做：

```
[pass] regions=50 changed=20 restored=20   ← 每 ~250ms 一次
```

三个 bug 叠在一起：

- `isStale` 对「片假名开头」的歌词行恒为真：这种形态注入时原文本节点会被移除
  （`kept=false`），而判断里有 `node.parentNode !== host`，永远成立，
  于是每轮都判定失效、还原重注一遍。这就是抽搐的直接原因。
- `orphaned` 把「我们插的注音节点不见了」当成「记录作废」：宿主还在、
  只是 React 摘掉了注音（最常见的情况）却被丢掉记录，那一行再也标不回来
  （restore 完记录已删，同一轮不会再注一次）。
- `pass` 先拍文本节点快照再还原，快照拿到的是切短的半截文本，还原后没人再看它。

稳定性判据换成「可见原文（剔除 `<rt>` 后的底字）有没有变」——
不看 `isConnected`（React 每 250ms 重建歌词行，节点时连时断）。
底字没变就一律不动 DOM。另外 `restore` 恢复 `kept=false` 形态时改为按
注入前记下的子节点下标插回，避免 React 已换行时把旧节点插回去渲染两遍。

验证：5 行歌词 + 播放栏的收敛脚本里 `pass1 changed=4`、
`pass2..6 changed=0 restored=0`，完全收敛。

测试 53 → 56。

## 1.0.5

修「启用后网易云提示应用出错」。根因是**拿整个 `body` 当扫描区域**：
轨迹显示 `regions=1`（就是 BODY），侧边栏、搜索框、歌单名、评论正文
全被改写，一轮就改了 8 处，直接把应用干到错误页。

- `findRegions("safe"|"lyrics")` 改用白名单选择器，**永远不返回 `document.body`**；
  白名单 = 歌词容器 + 播放栏的歌曲名/歌手。宁可漏标，不可越界。
- 连续 5 轮扫描出错就自动停用并还原 DOM（电路熔断）。
- `tools/read-trace.js`：localStorage 的值是 UTF-16LE，之前按 UTF-8 解所以读不到；
  改用 UTF-16LE 的 `["` 字节特征定位，逐文件小窗口解码（一次性解码大文件会让
  进程 `STATUS_HEAP_CORRUPTION` 崩掉）。

测试 52 → 53。

## 1.0.4

1.0.3 的改动（不再替换 React 的文本节点）没能解决「应用出错了」，
而渲染进程的控制台异常读不到（native 日志 `cloudmusic.elog` 不含它），
所以这一版先把**取证**做好，不再靠猜：

- 新增运行轨迹信标：插件把启动信息、每轮扫描结果、注音明细、`pass` 异常、
  以及 `window.onerror` / `unhandledrejection` 抓到的东西写进
  `localStorage['katakana-terminator.trace']`（最多 250 行，写失败绝不影响插件）。
  落盘在网易云的 Local Storage 里，可以用 `node tools/read-trace.js` 读出来。
- 新增 `tools/cdp-capture.js`：带 `--remote-debugging-port=9222` 启动网易云后，
  直接读渲染进程的 console 与未捕获异常。
- 检测 BetterNCM 安全模式（`betterncm.safemode`），安全模式下完全不动 DOM。

## 1.0.3

修「启用插件后网易云提示『应用出错了…重启下试试吧』」。

根因是 DOM 注入方式对 React 不友好：旧实现把整个文本节点替换成新的节点树
（`insertBefore(fragment)` + `removeChild(node)`）。React 更新纯文本时是在
它自己持有的那个文本节点上执行 `setTextContent(node)`，节点一旦被我们换掉，
这个引用就失效了，commit 阶段可能抛错，整页掉进网易云自己的错误页。
默认的「标注全部」会把整页 body 都扫一遍，命中面越大越容易踩到。

改成只「切短」原文本节点、注音作为兄弟节点插在它后面：

- 原文本节点 `nodeValue` 只被改写、不被移除，React 的引用始终有效；
- 注音和文本分段都记进 `inserted`，还原时逐个摘掉，原文从 `rec.plain` 写回，
  所以「还原后与原文逐字节一致」和「不残留重影」都有测试保证；
- 只有文本正好以片假名词开头时（第 0 段本身要带注音）才移除原节点，
  避免底字渲染两遍 —— 这种形态有单独的回归测试。

另外加了两个兜底，避免以后再出现「崩了但没法自救」：

- `MutationObserver` 回调整体 try/catch，回调里出错不再外泄成未捕获异常；
- 紧急开关：控制台执行 `localStorage['katakana-terminator.off'] = '1'`
  （不重启也生效，最多等一轮扫描），插件立刻停手并还原 DOM。
  设置面板在崩溃时是打不开的，所以这个开关不依赖 UI。

## 1.0.2

- 修 CI：`actions/setup-node` 从 Node 20 升到 22.22.2。
  jsdom 30 要求 `^22.22.2 || ^24.15.0 || >=26`，在 Node 20 上它依赖的 undici
  会崩（`TypeError: webidl.util.markAsUncloneable is not a function`），
  导致四个测试文件全部 `testCodeFailure`。
- `package.json` 的 `engines.node` 改为 `>=22.22.2`，并加 `.npmrc`（`engine-strict=true`），
  让版本不满足时在 `npm install` 阶段就失败，而不是等到跑测试才报一堆看不懂的错。
- `tools/check.js` 新增「运行时版本」检查项，提前拦这类问题。
- CI 的测试步骤去掉 `|| npm run test:serial` 兜底 —— 它会把真实失败掩盖成通过。

## 1.0.1

- 填入维护者信息与仓库地址（`manifest.json` 的 `author` / `author_link`，`main.js` 的 `REPO_URL`），
  设置面板里的「源码仓库 / 反馈问题」现在指向真实地址。
- 在线翻译的候选接口从 2 个扩到 4 个：除 `dict-chrome-ex` 的两个 host 外，
  补上 `client=gtx`（`/translate_a/single`）形状的候选，并支持解析该形状的响应。
- 新增 `tools/live-check.js`（联网自测）与 `KT` 控制台调试对象。
- `tools/check.js` 增加元信息一致性检查：`REPO_URL` 必须与仓库一致、不得残留 `OWNER` 占位符。

## 1.0.0

首个版本。

- 把 [Katakana Terminator](https://github.com/Arnie97/katakana-terminator) 的用途搬到网易云音乐：
  片假名外来语上方标注英文原词。
- 三级翻译来源：会话缓存 → 离线词典（335 条）→ 在线翻译（Google `dict-chrome-ex` 接口）。
  原版完全依赖在线接口，这里保证断网也能用。
- 在线部分攒批（约 1.2 秒 / 每批 50 词）、去重、记失败不重试，翻译结果持久化缓存 30 天。
- 默认标注歌词与标题/歌手/专辑，可在设置里改成只标歌词或自定义选择器。
- 实测内核是否支持 ruby 排版，不支持时用绝对定位降级。
- 与 React 协作：注入前快照宿主子节点，还原时整批放回；能识别元素被重建并清理孤儿注音。
